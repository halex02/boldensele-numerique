let index = document.querySelectorAll('[data-index="index-nominum"]');

let mouseOverHandler = function (e){
    let identifier = e.nextSibling();
    let popup = document.createElement("span");
    popup.setAttribute("class" "popup");
    e.insertAdjacentElement("afteren", popup)
}

let mouseOutHandler = function (e) {
    e.
}

let mouseOutHandler = function ()
foreach (let id in index){
    id.addEventListener("mouseover", mouseOverHandler);
    id.addEventListener("mouseout", mouseOutHandler);
}
